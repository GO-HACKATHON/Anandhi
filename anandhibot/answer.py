class Answer:
	def __init__(self, docID, title, url, key, pattern, priority, keywordOccurence):
		self.docID = docID
		self.title = title
		self.url = url
		self.key = key
		self.pattern = pattern
		self.priority = priority
		self.keywordOccurence = keywordOccurence
